/// <summary>
/// The AggroZoneMarker class is responsible for marking areas where enemies can detect and become aggressive towards the player.
/// This script can be attached to game objects to define aggro zones in the game world.
/// </summary>
using UnityEngine;

/// <summary>
/// Represents an aggro zone marker in the game world.
/// </summary>
public class AggroZoneMarker : MonoBehaviour
{
    /// <summary>
    /// Initializes the aggro zone marker when the script is loaded.
    /// This method is called once before the first execution of Update.
    /// </summary>
    void Start()
    {
        
    }

    /// <summary>
    /// Updates the aggro zone marker every frame.
    /// This method is called once per frame and can be used for continuous logic.
    /// </summary>
    void Update()
    {
        
    }
}
